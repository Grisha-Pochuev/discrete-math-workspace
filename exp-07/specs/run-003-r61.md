# Run 003 — r61 modular orbit certificate

This run targets the lambda=9 elliptic branch only. It does not claim a global proof of the original problem.

Mathematical model:

- E: Y^2 = X^3 - 48;
- generator direction G=(4,-4);
- cube-class condition f(P)=-(Y-4)(Y^2-24Y+336)/7 in Q^3.

For every good prime p == 1 (mod 3), p != 7, a rational cube f(nG) must reduce either to 0, a cubic residue, or a pole (the point at infinity). Therefore any prime for which the finite reduction is a non-cubic residue is an exact obstruction.

The worker first replays an audited wheel from primes

367, 1531, 3943, 163, 1429, 991, 4027, 151, 271, 1291, 1741

and must reproduce exactly

- modulus W = 16943706;
- allowed residues 0, 1, 822511, W-1.

It then checks remaining wheel candidates against the 128 most selective exact filters from primes p <= 20000, p == 1 (mod 3).

Full launch parameters:

- signed coefficient range: 2 <= |n| <= 18446744073709551615 (=2^64-1);
- 20 GitHub-hosted workers;
- partition: k mod 20 inside each of the four wheel progressions, with both signs checked;
- soft worker limit: 19800 seconds;
- encrypted detailed artifacts using exp-07/k0.pub;
- collector independently recomputes each worker's exact expected signed-index count.

A complete negative run means only that no coefficient in this finite range survives all selected necessary local cube conditions. A survivor exits nonzero and is retained in the encrypted details for exact follow-up. A timeout/partial shard is missing coverage, not a negative result.
