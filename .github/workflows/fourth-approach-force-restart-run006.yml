name: Fourth approach Run 006 stopped

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/fourth-approach-force-restart-run006.yml'

permissions:
  actions: read
  contents: read

concurrency:
  group: fourth-approach-force-restart-run006
  cancel-in-progress: true

jobs:
  stopped:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Run 006 is intentionally stopped.'
