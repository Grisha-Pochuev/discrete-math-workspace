name: x62-h

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/x62-h.yml'
      - 'x62/h.py'

permissions:
  contents: read

jobs:
  r:
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        include:
          - {id: 0, seed: 1301, order: 0, mode: 0, form: 0, chunk: 24, k: 0}
          - {id: 1, seed: 1409, order: 1, mode: 0, form: 0, chunk: 32, k: 0}
          - {id: 2, seed: 1511, order: 2, mode: 0, form: 0, chunk: 24, k: 0}
          - {id: 3, seed: 1601, order: 3, mode: 3, form: 0, chunk: 40, k: 0}
          - {id: 4, seed: 1709, order: 0, mode: 1, form: 0, chunk: 24, k: 0}
          - {id: 5, seed: 1801, order: 2, mode: 2, form: 0, chunk: 32, k: 0}
          - {id: 6, seed: 1901, order: 0, mode: 5, form: 0, chunk: 24, k: 0}
          - {id: 7, seed: 2003, order: 2, mode: 6, form: 0, chunk: 24, k: 0}
          - {id: 8, seed: 2111, order: 0, mode: 7, form: 0, chunk: 24, k: 0}
          - {id: 9, seed: 2203, order: 2, mode: 8, form: 0, chunk: 32, k: 0}
          - {id: 10, seed: 2309, order: 0, mode: 5, form: 1, chunk: 24, k: 0}
          - {id: 11, seed: 2411, order: 2, mode: 7, form: 1, chunk: 32, k: 0}
    name: h2-${{ matrix.id }}
    runs-on: ubuntu-24.04
    timeout-minutes: 36
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            x62
          sparse-checkout-cone-mode: true
          fetch-depth: 1
      - name: r
        shell: bash
        run: |
          set -euo pipefail
          python3 -m venv .v
          . .v/bin/activate
          python -m pip install -q --disable-pip-version-check ortools
          set +e
          timeout 29m python x62/h.py x62/d \
            ${{ matrix.seed }} 1700 out \
            ${{ matrix.order }} ${{ matrix.mode }} ${{ matrix.form }} \
            ${{ matrix.chunk }} ${{ matrix.k }}
          rc=$?
          set -e
          tail -40 out || true
          if [ "$rc" -ne 0 ] && [ "$rc" -ne 2 ] && [ "$rc" -ne 124 ]; then
            exit "$rc"
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: h2-${{ matrix.id }}
          path: out
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  a:
    needs: r
    if: always()
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: h2-*
          path: z
          merge-multiple: false
      - name: a
        shell: bash
        run: |
          python3 - <<'PY'
          import glob, os
          fs=sorted(glob.glob('z/h2-*/out'))
          print('files',len(fs))
          found=[]
          for p in fs:
              lines=open(p,encoding='utf-8').read().splitlines()
              status=next((x for x in lines if x.startswith('status=')),'status=MISSING')
              reached=next((x for x in lines if x.startswith('reached=')),'reached=FOUND')
              phases=next((x for x in lines if x.startswith('phases=')),'phases=?')
              print(os.path.basename(os.path.dirname(p)),status,reached,phases)
              if status=='status=FOUND': found.append(p)
          print('found',len(found))
          for p in found:
              print('RESULT',p)
              print(open(p,encoding='utf-8').read())
          PY
