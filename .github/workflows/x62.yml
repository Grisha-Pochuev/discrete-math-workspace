name: x62

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'x62/**'
      - '.github/workflows/x62.yml'

permissions:
  contents: read

concurrency:
  group: x62-main
  cancel-in-progress: false

jobs:
  prep:
    name: prep
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            x62
          sparse-checkout-cone-mode: true
          fetch-depth: 1

      - name: check
        shell: bash
        run: |
          set -euo pipefail
          test -s x62/b.py
          test -s x62/e.py
          test -s x62/d
          test "$(wc -l < x62/d)" -eq 1029
          echo "fd7f67051b817842f5795fae812b852efa8007f5478a286365a7236a6a790cd8  x62/d" | sha256sum -c -
          python3 -m py_compile x62/b.py x62/e.py
          python3 x62/b.py x62/d check

      - name: deps
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p x62/w
          for attempt in 1 2 3; do
            rm -f x62/w/*
            if python3 -m pip download --disable-pip-version-check --only-binary=:all: --dest x62/w ortools==9.15.6755; then
              exit 0
            fi
            if [ "$attempt" -eq 3 ]; then
              echo "::error::dependency download failed after 3 attempts"
              exit 1
            fi
            sleep $((attempt * 15))
          done

      - name: smoke
        shell: bash
        run: |
          set -euo pipefail
          rm -rf x62/s
          python3 -m pip install --disable-pip-version-check --no-index --find-links x62/w --target x62/s ortools==9.15.6755
          PYTHONPATH="$PWD/x62/s" python3 -c 'import ortools; print(ortools.__version__)'
          set +e
          PYTHONPATH="$PWD/x62/s:$PWD/x62" python3 x62/e.py x62/d 6205999 40 x62/t a 0 0
          rc=$?
          set -e
          if [ "$rc" -ne 0 ] && [ "$rc" -ne 2 ]; then
            echo "::error::smoke exited with code $rc"
            exit "$rc"
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: x62-runtime
          path: |
            x62/b.py
            x62/e.py
            x62/d
            x62/w
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  r:
    needs: prep
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix:
        include:
          - {id: 0,  seed: 6205100, order: a, mode: 0, fix: 0}
          - {id: 1,  seed: 6205101, order: a, mode: 0, fix: 0}
          - {id: 2,  seed: 6205102, order: a, mode: 0, fix: 0}
          - {id: 3,  seed: 6205103, order: a, mode: 0, fix: 0}
          - {id: 4,  seed: 6205104, order: a, mode: 0, fix: 0}
          - {id: 5,  seed: 6205105, order: a, mode: 0, fix: 0}
          - {id: 6,  seed: 6205106, order: a, mode: 0, fix: 0}
          - {id: 7,  seed: 6205107, order: a, mode: 0, fix: 0}
          - {id: 8,  seed: 6205110, order: d, mode: 0, fix: 0}
          - {id: 9,  seed: 6205111, order: d, mode: 0, fix: 0}
          - {id: 10, seed: 6205112, order: d, mode: 0, fix: 0}
          - {id: 11, seed: 6205113, order: d, mode: 0, fix: 0}
          - {id: 12, seed: 6205114, order: d, mode: 0, fix: 0}
          - {id: 13, seed: 6205115, order: d, mode: 0, fix: 0}
          - {id: 14, seed: 6205120, order: r, mode: 0, fix: 0}
          - {id: 15, seed: 6205121, order: r, mode: 0, fix: 0}
          - {id: 16, seed: 6205122, order: r, mode: 0, fix: 0}
          - {id: 17, seed: 6205130, order: a, mode: 1, fix: 0}
          - {id: 18, seed: 6205131, order: a, mode: 2, fix: 0}
          - {id: 19, seed: 6205139, order: a, mode: 0, fix: 8}

    name: ${{ matrix.id }}
    runs-on: ubuntu-24.04
    timeout-minutes: 14
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: x62-runtime
          path: x62

      - name: run
        shell: bash
        run: |
          set -euo pipefail
          rm -rf x62/s
          python3 -m pip install --disable-pip-version-check --no-index --find-links x62/w --target x62/s ortools==9.15.6755
          set +e
          PYTHONPATH="$PWD/x62/s:$PWD/x62" python3 x62/e.py x62/d ${{ matrix.seed }} 600 x62/z ${{ matrix.order }} ${{ matrix.mode }} ${{ matrix.fix }} >x62/l 2>&1
          rc=$?
          set -e
          cat x62/l
          if [ "$rc" -ne 0 ] && [ "$rc" -ne 2 ]; then
            echo "::error::x62 exited with code $rc"
            exit "$rc"
          fi
          exit 0

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: x62-${{ matrix.id }}
          path: |
            x62/z
            x62/l
          if-no-files-found: ignore
          retention-days: 1
          compression-level: 0
