name: x62

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/x62.yml'

permissions:
  contents: read

concurrency:
  group: x62-main
  cancel-in-progress: false

jobs:
  k:
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        include:
          - {s: 101,   k: 515, f: 160, o: 0, m: 0, b: 8}
          - {s: 1009,  k: 515, f: 192, o: 1, m: 1, b: 8}
          - {s: 10007, k: 515, f: 224, o: 2, m: 2, b: 8}
          - {s: 20011, k: 343, f: 96,  o: 0, m: 1, b: 8}
          - {s: 30013, k: 343, f: 128, o: 1, m: 2, b: 8}
          - {s: 40009, k: 251, f: 24,  o: 2, m: 0, b: 8}
    name: k-${{ matrix.s }}
    runs-on: ubuntu-24.04
    timeout-minutes: 42
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            x62
          sparse-checkout-cone-mode: true
          fetch-depth: 1
      - name: d
        shell: bash
        run: python3 -m pip install -q ortools==9.14.6206 scipy==1.16.1
      - name: r
        shell: bash
        run: |
          set +e
          timeout 37m python3 x62/k.py x62/d ${{ matrix.s }} 2160 x62/r ${{ matrix.k }} ${{ matrix.f }} ${{ matrix.o }} ${{ matrix.m }} ${{ matrix.b }} >x62/stdout 2>&1
          rc=$?
          set -e
          cat x62/stdout || true
          cat x62/r || true
          if grep -q '^FOUND$' x62/r 2>/dev/null; then exit 0; fi
          if [ "$rc" -eq 124 ] || [ "$rc" -eq 2 ]; then exit 0; fi
          exit "$rc"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k-${{ matrix.s }}
          path: |
            x62/r
            x62/stdout
          if-no-files-found: warn
          retention-days: 1
          compression-level: 0
