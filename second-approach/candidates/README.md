# Candidate seed bank

`seed-bank.json.gz` is generated by the collector after the first compute run. It retains the strongest complete numerical candidates from earlier runs so later runs can refine or mutate them.

The seed bank is derived data. The authoritative records remain in `second-approach/runs/`.
